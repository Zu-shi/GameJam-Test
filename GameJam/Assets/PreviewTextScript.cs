using UnityEngine;
using System.Collections;

public class PreviewTextScript : MonoBehaviour {
	private TextEditor previewText;
	// Use this for initialization
	void Start () {
		previewText = GetComponent<TextEditor> ();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
